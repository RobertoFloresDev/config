# - Set Env Vars (EOF > execute and replaces $(COMMAND) in file)
# tee /opt/stacks/zerobyte/.env <<EOF
BASE_URL=https://zerobyte.<server>.<user>.local.<domain>.com
APP_SECRET=$(openssl rand -hex 32)
# EOF
